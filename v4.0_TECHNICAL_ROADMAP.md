# [寂 · JÌ] v4.0 “至臻化境” 技术实现方案

## 1. 核心架构：零源码无痕渲染引擎 (P1-P2)
*   **目标**：彻底消除编辑过程中的 Markdown 符号痕迹，实现类似 Typora 的“写完即隐”。
*   **技术路径**：
    *   **块级转换**：弃用简单的正则替换，采用基于 `Selection` 与 `MutationObserver` 的局部 DOM 树重构。
    *   **语法全覆盖**：
        *   标题：H1-H6 (# - ######)
        *   引用：`> `
        *   高亮块 (Callouts)：支持 `:::info`, `:::success`, `:::warning`, `:::danger` 以及 GitHub 风格的 `[!NOTE]` 等。支持跨行内容包裹。
        *   任务列表：`- [ ] ` 与 `- [x] `
        *   行内样式：加粗 (`**`), 高亮 (`==`), 下标 (`~`), 上标 (`^`), 行内/块级公式 (`$ / $$` KaTeX)。
    *   **稳定性**：确保在渲染转换瞬间，光标定位精确至 1 像素，支持原生 `Undo/Redo`。

## 2. 影像系统：视觉化交互与持久化 (P3-P4)
*   **目标**：支持图片的直接粘贴与等比例交互缩放。
*   **技术路径**：
    *   **插入逻辑**：拦截 `paste` 事件，读取 `clipboardData` 中的图片 Blob，转换为 Base64。
    *   **四点缩放**：点击图片后，显示 4 个圆形控制柄（Handles）。通过监听 `mousemove` 实现等比例缩放。
    *   **本地存储**：利用 `IndexedDB` (CyberZenDB) 存储大体积图片数据，确保刷新页面后图片不丢失。

## 3. 云管理与持久化体系 (P6-P8)
*   **目标**：建立文档管理、热保存、账号体系与导入导出。
*   **技术路径**：
    *   **数据主权**：基于 `IndexedDB` 存储多文档及编辑历史。
    *   **热保存**：采用 800ms 防抖策略，实时静默保存陛下的每一行墨宝。
    *   **主权流转**：支持 `.md` 文件的双向导入与导出，确保陛下对数字资产的绝对主权。
    *   **账号体系**：预留极简登录 UI，为后续多端同步方案（WebDAV/GitHub）打桩。

## 4. 高级交互系统 (P9-P10)
*   **目标**：全能生产力增强。
*   **技术路径**：
    *   **斜杠指令 (Slash Commands)**：输入 `/` 即刻在光标处唤起浮动面板。支持标题、表格、代码块、高亮块、AI 等。
    *   **动态表格系统**：实现可视化表格编辑。点击单元格即显示 `+R/-R/+C/-C` 浮动控制柄。
    *   **查找替换**：`Cmd+F` 唤起极简浮层，支持全站搜索与一键更替。
    *   **评论系统**：选中文字后，右侧边缘浮现“气泡”，支持陛下对文字进行批注并持久化。
    *   **AI 灵感 (Cmd+E)**：设计赛博蓝微光指令栏，预留 DeepSeek API 流式接口，支持针对选区的润色、续写。

## 5. 禅意美学与细节 (P5)
*   **常驻大纲**：固定于右侧，默认 `opacity: 0.15`，悬停时平滑显化。
*   **至臻占位符**：编辑器空无一字时，通过 CSS 伪类显示灰色提示文字：*“在此入定，静待笔墨”*。不再使用“欢迎来到赛博禅龛”等硬编码文字。
*   **打字机模式**：视线中位锁定（45% 触发滚动），确保陛下始终“抬头写字”，保持笔耕心流。
*   **禅意彩蛋**：背景呼吸律动（每分钟 12 次起伏）及随字数增长的水墨竹影。

---
**尚书令 · 监制**
