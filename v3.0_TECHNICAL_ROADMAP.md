# [寂 · JÌ] v3.0 “大隐于市” 技术实现方案

## 1. 核心架构：零源码无痕渲染引擎 (P1-P2)
*   **目标**：彻底消除编辑过程中的 Markdown 符号痕迹，实现类似 Typora 的“写完即隐”。
*   **技术路径**：
    *   **块级转换**：弃用简单的正则替换，采用基于 `Selection` 与 `MutationObserver` 的局部 DOM 树重构。
    *   **语法全覆盖**：
        *   标题：H1-H6 (# - ######)
        *   引用：`> `
        *   高亮块 (Callouts)：`:::info`, `:::success`, `:::warning`, `:::danger`, `:::tips`
        *   任务列表：`- [ ] ` 与 `- [x] `
        *   行内样式：加粗 (`**`), 高亮 (`==`), 删除线 (`~~`), 上标 (`^`), 下标 (`~`), 链接 (`[text](url)`)
    *   **稳定性**：确保在渲染转换瞬间，光标定位精确至 1 像素，支持原生 `Undo/Redo`。

## 2. 影像系统：视觉化交互与持久化 (P3-P4)
*   **目标**：支持图片的直接粘贴与等比例交互缩放。
*   **技术路径**：
    *   **插入逻辑**：拦截 `paste` 事件，读取 `clipboardData` 中的图片 Blob，转换为 Base64。
    *   **四点缩放**：点击图片后，动态生成覆盖层，提供 4 个控制柄（Handles）。通过监听 `mousemove` 实现等比例缩放，并实时更新图片的 `width` 属性。
    *   **本地存储**：利用 `IndexedDB` 存储大体积图片数据，确保刷新页面后图片不丢失，且不依赖任何外部服务器。

## 3. 数据主权：Markdown 逆向序列化 (P6)
*   **目标**：在富文本编辑状态下，复制出的内容自动还原为 Markdown 源码。
*   **技术路径**：
    *   拦截全局 `copy` 事件。
    *   遍历编辑器内部 DOM 树，根据节点类型（如 `H1`, `BLOCKQUOTE`, `CALLOUT`）递归生成标准的 Markdown 字符串。
    *   将纯文本 Markdown 注入剪贴板，确保与其他 Markdown 编辑器（Typora/Notion）完美兼容。

## 4. 极简 UI 与 帮助系统 (P5)
*   **目标**：极低存在感的辅助系统。
*   **技术路径**：
    *   **潜行目录**：固定于右侧，默认 `opacity: 0.1`，悬停时平滑显化。
    *   **帮助法典**：左下角极简“？”按钮，展示全语法对照表。
    *   **响应式适配**：通过媒体查询（Media Queries）确保在 `720px`（窄屏）到 `1200px`（宽屏）下均有最佳间距。

## 5. 禅意彩蛋 (P7)
*   **墨色竹影**：基于 Canvas 的生成算法，根据字数里程碑（500/1000/3000字）缓缓绘制。
*   **背景呼吸**：利用 CSS 动画实现背景色在极细微范围内的明度起伏。

---
**工部尚书 · 监制**
